# Rapport SEO COO — Run 1, 2026-08-11

## 1. Décision de la semaine

**REFRESH-repositionnement de `/blog/long-pomodoro-sessions-guide` en page canonique « Best Pomodoro Time »** (commit local `dc9088b`, NON poussé, en attente de ta relecture).

Justification chiffrée :
- La page est à 917 impressions (+711 en 28 j), position 23, 2 clics : Google la teste massivement sur le cluster « pomodoro intervals / how long / best time » (~25 variantes en positions 20-90) mais la classe mal car elle présupposait « allonge tes sessions » au lieu de répondre à la question neutre.
- Volumes OpenSEO US : best pomodoro time 720/mois KD 22, pomodoro 50 10 210 KD 0, best pomodoro intervals 90 KD 21, how long is a pomodoro 70 KD 20, how many pomodoros before long break 70 KD 19. Cluster ≈ 1 300/mois, compétition LOW partout.
- Candidats écartés : création d'un article dédié (aurait cannibalisé cette page), refresh ultradian (reporté run 2), listicle body doubling app (cluster trop récent, J+28).

Ce qui a changé dans l'article (URL et substance conservées) :
- Title : « Long Pomodoro Sessions: When 25 Minutes Isn't Enough » → « Best Pomodoro Time: How Long Should Your Intervals Be? »
- Intro answer-first (réponse dans les 100 premiers mots) + tableau de décision 25/5 vs 45-60 vs 90 par type de tâche.
- Nouvelle section « How Many Pomodoros Before a Long Break? » (requête 70/mois absente de la page ; tie-in réglage long break interval de Pomodorian).
- 2 nouvelles FAQ alignées sur les formulations GSC exactes ; +2 liens internes (ultradian, breaks) ; keywords et updated à jour.

## 2. FIX appliqué (même commit local)

`/blog/use-pomodoro-breaks-effectively` (1 168 impressions, +790, pos 15, 0 clic — déjà au standard depuis le 27/07) : title/meta alignés sur la formulation questionnée « What to Do During Pomodoro Breaks: 11 Science-Backed Ideas » (~20 variantes GSC + 170/mois KD 0). Contenu inchangé.

## 3. FIX proposés, non appliqués (run 2, regarder la SERP d'abord)

- `/blog/best-pomodoro-apps-2026` : position 4,3 (améliorée), 87 impressions, 0 clic → title/meta CTR.
- `/pomodorian-vs-pomofocus` : position 3,9, 75 impressions, 0 clic → title/meta CTR.

## 4. Tableau de bord (GSC 28 j vs 28 j précédents)

- Clics : 29 vs 26 (+11,5 %). Requêtes marque ~7 clics ; hors marque le reste.
- Top mover : `group-pomodoro-team-sessions` +6 clics, pos 10,5 (la piste body doubling travaille).
- Alertes : home pos 7,5 → 11,4 (clics -5) ; ultradian EN pos 7,5 → 21,4 malgré +92 impressions (cluster monte en 4 langues) ; `micro-breaks-boost-focus` -405 impressions (chevauche l'article breaks, candidat MERGE) ; `pair-programming-vs-solo-deep-work` -146.
- État complet : `seo-agent/ETAT-ARTICLES.tsv` (6 REFRESH, 6 STAR, 2 FIX, 36 NEW, 91 DORMANT).
- Budget OpenSEO consommé ce run : ~16-32 crédits (projet « Pomodorian » US/en créé, id dans JOURNAL.md).

## 5. Ce que tu dois faire (~20 min)

1. Relire le diff du commit local `dc9088b` (2 fichiers) : `git show dc9088b`.
2. Ajuster si besoin, puis `git push` → deploy Vercel.
3. GSC : demande d'indexation sur `/blog/long-pomodoro-sessions-guide` et `/blog/use-pomodoro-breaks-effectively`.
4. (Optionnel) Me dire GO pour décliner le refresh sur les traductions fr/es/de (es est aussi REFRESH : pos 8,4, 75 impressions).

Prochain run : lundi 18/08 (`/seo-coo` dans ~/dev/pomodorian). Au programme probable : refresh ultradian EN, FIX CTR des 2 pages money, lecture J+7 de ce refresh.
